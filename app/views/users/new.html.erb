<h1>New user</h1>

<%= render("shared/breadcrumb", breadcrumbs: [
  { name: "Users", path: users_path },
  { name: "New User" }
]) %>

<div class="card">
  <div class="card-body">
    <%= render("form", user: @user) %>
  </div>
</div>
